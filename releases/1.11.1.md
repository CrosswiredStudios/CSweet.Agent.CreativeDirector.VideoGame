# 1.11.1

## Resume Producer planning

- Propose the packaged video-game production profile at revision 5. The previous revision 4 request was rejected because only revision 5 is active, stopping project setup before the Producer handoff.
- Give new portfolio-review tasks their source conversation. Existing review tasks that lack that field recover it from their stable project correlation and are requeued when they were blocked for this reason.
- Requeue the existing Producer-documentation handoff task during project reconciliation when the documents were accepted but an earlier setup attempt failed.
- No new capabilities, credentials, configuration, or network access are requested.

After deploying this version, the Director's next attention review can resume project setup. A human must still approve the governed workstream proposal; the Director and Producer then refine the production brief before the Producer submits staffing for the Director's review.
