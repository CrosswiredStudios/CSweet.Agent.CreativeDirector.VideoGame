# 1.10.1

## Startup identity and onboarding recovery

- Correct the runtime identity reported by Naomi Chen to match the approved package version. The broker previously rejected the mismatched startup handshake, leaving the durable onboarding introduction undelivered.
- Verify the published agent's ID and version against its packaged manifest and assembly before the isolated builder accepts a release. A future mismatch now fails the build instead of becoming a hired but offline employee.
- No new capabilities, grants, configuration, or network access are requested.

Existing installations need this new package deployed and their suppressed startup retried. The original onboarding event remains durable and is delivered to the updated installation.
